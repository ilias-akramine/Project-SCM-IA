the real Solomon benchmark files are here:

- C101.txt
- R101.txt
- RC101.txt

The project reads real Solomon instances instead of generating synthetic random data.
Expected run command:

    cd code
    python main.py
