Put the real Solomon benchmark files here:

- C101.txt
- R101.txt
- RC101.txt

The project now reads real Solomon instances instead of generating synthetic random data.
Expected run command:

    cd code
    python main.py

Notes:
- If you want a faster demo run, edit main.py and set customer_limit=25.
- For the final project, keep full 100-customer instances whenever possible.
